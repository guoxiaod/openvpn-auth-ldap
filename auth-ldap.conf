<LDAP>
	# LDAP server URL
	URL		ldap://ldap1.example.org

	# Bind DN (If your LDAP server doesn't support anonymous binds)
	# BindDN		uid=Manager,ou=People,dc=example,dc=com

	# Bind Password
	# Password	SecretPassword

	# Network timeout (in seconds)
	Timeout		15

	# Enable Start TLS
	TLSEnable	yes

	# Follow LDAP Referrals (anonymously)
	FollowReferrals yes

	# TLS CA Certificate File
	TLSCACertFile	/usr/local/etc/ssl/ca.pem

	# TLS CA Certificate Directory
	TLSCACertDir	/etc/ssl/certs

	# Client Certificate and key
	# If TLS client authentication is required
	TLSCertFile	/usr/local/etc/ssl/client-cert.pem
	TLSKeyFile	/usr/local/etc/ssl/client-key.pem

	# Cipher Suite
	# The defaults are usually fine here
	# TLSCipherSuite	ALL:!ADH:@STRENGTH
</LDAP>

<Authorization>
	# Base DN
	BaseDN		"ou=People,dc=example,dc=com"

	# User Search Filter
	SearchFilter	"(&(uid=%u)(accountStatus=active))"

	# Require Group Membership
	RequireGroup	false

	# Add non-group members to a PF table (disabled)
	#PFTable	ips_vpn_users

    #OATHEnable         true
    #<OATH>
    #    # digits=123456&period=30&type=totp&algorithm=sha1&account=xxxx&&issuer=xxxxxx&sign=xxxx
    #    # sign = hash("sha256", secret_key + key1=val1&key2=val2&key3=val3 + time) + time
    #    OATHApi            https://example.com/a.php?x=h+b
    #    OATHSignEnable     true
    #    OATHSecret         123456789
    #    OATHIssuer         "OpenVPN"
    #    OATHPeriod         30
    #    OATHType           totp
    #    OATHDigitsLength   6
    #    OATHAlgorithm      SHA1
    #    OATHTimeout        3
    #    OATHConnectTimeout 3
    #</OATH>

	<Group>
		BaseDN		"ou=Groups,dc=example,dc=com"
		SearchFilter	"(|(cn=developers)(cn=artists))"
		MemberAttribute	uniqueMember
		# Add group members to a PF table (disabled)
		#PFTable	ips_vpn_eng
	</Group>
</Authorization>
